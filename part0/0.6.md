```mermaid
sequenceDiagram
    participant browser
    participant server

    Note right of browser: User writes note and clicks save

    browser->>browser: JavaScript creates note object
    browser->>browser: Browser updates notes list on page

    browser->>server: POST /new_note_spa
    activate server
    server-->>browser: Status 201 Created
    deactivate server

    Note right of browser: No page reload happens
```